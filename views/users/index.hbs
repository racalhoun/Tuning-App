
    <div> 
    <h1>Users:</h1>
    <div>
        <a href="/users/new">Add a New User</a>
    </div>
    
    {{#each users}}
    <div>
       <ul>
          {{this.name}} - {{this.stable}}
            
        
        </ul>
    </div>          
            <!-- <a href="/users/{{this._id}}">{{this.name}}Info</a>-->
            <!--<a href="/users/{{this._id}}/cars">{{this.name}}'s Cars</a>-->
           {{/each}} 
           
            <a href="/users/{{this._id}}/edit">Edit</a>
            <a href="/users/{{this._id}}/delete">Delete</a> 
        
        

   
  </div>